<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>What's Good Columbus</title>
  <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="site-background">
  <div class="flex-column justify-space-around">
    <header class="display-flex justify-space-around align-center p-2">
      <h1>
        <span role="img" aria-label="money"><img class="ohio" src="/assets/Ohio_white.png"/></span>
        <a href="/" class="header-text">What's Good Columbus</a>
      </h1>
      <figure>
        <img src="/assets/columbus_sky_white.png" alt="Columbus skyline illustraion" height="60px">
      </figure>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile">profile</a> |
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login" class="header-text">login</a>
        {{/if}}
      </nav>
    </header>
    <main class="container container-fluid mt-3">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>
      
    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
